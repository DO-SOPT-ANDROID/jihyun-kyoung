<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoSoptTemplate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DoSoptTemplate" parent="Base.Theme.DoSoptTemplate" />
    <style name = "ErrorTextAppearance" parent = "Theme.DoSoptTemplate">
        <item name = "android:textColor">@color/red</item>
        <item name = "boxStrokeColor">@color/red</item>
    </style>
    <style name = "normalTextAppearance" parent = "Theme.DoSoptTemplate">
        <item name = "android:textColor">@color/red</item>
        <item name = "boxStrokeColor">@color/red</item>
    </style>
</resources>